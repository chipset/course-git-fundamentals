# Reset Modes

- `--soft`: move HEAD, keep changes staged.
- `--mixed`: move HEAD, keep changes unstaged. This is the default.
- `--hard`: move HEAD and discard matching working-tree changes.

Use `--hard` only when you are sure the commit is local and disposable.
